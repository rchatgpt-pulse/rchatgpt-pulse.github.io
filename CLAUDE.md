# CLAUDE.md

## Package Manager
- Always use **pnpm** (not npm or yarn).

## Styling
- Always use **Tailwind CSS** for styling.

## Deployment
- The site is deployed **statically to GitHub Pages**.
- No dynamic server-side components (no SSR, no server functions, no API routes that require a runtime server).
