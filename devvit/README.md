# Devvit app (scraper)

The `@devvit/web` app that scrapes r/ChatGPT and commits JSONL to the parent
repo each day. See [`../live/README.md`](../live/README.md) for the full live
monitor architecture, setup, and operational docs (the "Devvit app" section in
that file covers playtest, deploy, logs, settings, and the HTTP-route map).
